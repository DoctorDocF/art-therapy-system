[build]
  base = "."  # Корень репозитория
  command = "cd client && npm install && npm run build"
  publish = "client/build"  # Путь к собранному React-приложению

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
