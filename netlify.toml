[build]
  base = "client"  # Указываем базовую директорию
  command = "npm install && npm run build"
  publish = "build"  # Путь относительно базовой директории

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
